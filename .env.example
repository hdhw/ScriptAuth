# Server Configuration
NODE_ENV=development
PORT=3000

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here # Please change this in production you lazy fucks. I know you use this for a reason, Of course no offense, When i make stuff i forget to change the stuff to, So atleast change it in production if you will.
JWT_EXPIRES_IN=24h

# Database Configuration
DATABASE_URL=sqlite:./data/auth.db

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX=100

# Security
TRUST_PROXY=false

# Logging
LOG_LEVEL=info

# Security
TRUST_PROXY=1  # Set to 0 in development, 1 in production behind a proxy
